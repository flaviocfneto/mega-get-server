# Registry for hardened base image (dhi.io). Copy to .env and fill in.
# Get a PAT at https://hub.docker.com/settings/security
DHI_REGISTRY_USERNAME=
DHI_REGISTRY_TOKEN=

# Desktop app: download folder (optional; default is ~/Downloads). Used when .env is in project root.
# DOWNLOAD_DIR=/path/to/TEST-downloads
